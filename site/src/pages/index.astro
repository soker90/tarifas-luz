---
import Layout from "@/layouts/Layout.astro";
import TariffsApp from "@/components/TariffsApp";

const base = import.meta.env.BASE_URL.replace(/\/$/, "");
---

<Layout>
  <TariffsApp base={base} client:load />
</Layout>
